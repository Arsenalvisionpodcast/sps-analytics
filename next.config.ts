import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Clean static export for Vercel (Vercel also supports full Next.js SSR natively)
  // Remove the 'output' line below if you want SSR on Vercel
  // output: 'export',
};

export default nextConfig;
